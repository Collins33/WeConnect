language: python
python:
  - "3.6"
cache: pip
install:
  - pip install pytest
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install pytest-cov
script:
  - pytest
  
after_success: 
  - coveralls  